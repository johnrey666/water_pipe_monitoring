import 'package:flutter/material.dart';

class ViewBillingPage extends StatelessWidget {
  const ViewBillingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'View Billing Page',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.w500),
      ),
    );
  }
}
